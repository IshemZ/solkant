# =============================================================================
# SOLKANT - Environment Variables Template
# =============================================================================
# Copy this file to .env.local for development
# DO NOT create .env.production - use Vercel Dashboard instead
#
# IMPORTANT: Use DIFFERENT credentials for development and production
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (Supabase PostgreSQL)
# -----------------------------------------------------------------------------
# Get from: Supabase Dashboard → Project Settings → Database → Connection String
#
# ⚠️ PRODUCTION: Create a SEPARATE database project for production
#    - Development: devisio-dev
#    - Production: devisio-production

# Transaction mode (with connection pooling) - Used by Next.js app at runtime
# Port 6543 with pgbouncer=true for connection pooling
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true"

# Session mode (direct connection) - Used by Prisma CLI for migrations
# Port 5432 for direct database access
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@[PROJECT-REF].supabase.co:5432/postgres"

# -----------------------------------------------------------------------------
# NEXTAUTH CONFIGURATION
# -----------------------------------------------------------------------------
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
# ⚠️ PRODUCTION: Generate a NEW secret for production (never reuse dev secret)
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl"

# NEXTAUTH_URL is auto-detected by NextAuth in most cases
# Development: http://localhost:3000 (auto-detected)
# Production: Set in Vercel Dashboard to https://your-domain.vercel.app
# NEXTAUTH_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# GOOGLE OAUTH (Optional - for social login)
# -----------------------------------------------------------------------------
# Get from: Google Cloud Console → APIs & Services → Credentials
#
# ⚠️ PRODUCTION: Create SEPARATE OAuth credentials for production
#    Development credentials only work with http://localhost:3000
#    Production needs https://your-domain.vercel.app
#
# Create OAuth 2.0 Client ID and add authorized redirect URIs:
# DEVELOPMENT:
#   - Authorized JavaScript origins: http://localhost:3000
#   - Authorized redirect URIs: http://localhost:3000/api/auth/callback/google
#
# PRODUCTION:
#   - Authorized JavaScript origins: https://your-domain.vercel.app
#   - Authorized redirect URIs: https://your-domain.vercel.app/api/auth/callback/google

GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-client-secret"

# Supabase (Optional - if using Supabase features beyond database)
# Only needed if you're using Supabase Auth, Storage, or Realtime
# NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"
# NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
